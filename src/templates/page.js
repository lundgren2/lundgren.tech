import React from 'react'
import { graphql } from 'gatsby'
import Helmet from 'react-helmet'

const RootIndex = () => {
  return (
    <div style={{ background: '#fff' }}>
      <Helmet title="LD Gatsby Starter" />
    </div>
  )
}

export default RootIndex
